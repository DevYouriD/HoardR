spring:
  application:
    name: hoardr
  data:
    mongodb:
      uri: mongodb://mongodb:27017/cardcollectiondb
  graphql:
    graphiql:
      enabled: true

#    data:
#      mongodb:
#        uri: mongodb://root:root@localhost:27017/cardcollectiondb?authSource=admin

server:
  error:
    include-messages: always

logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG